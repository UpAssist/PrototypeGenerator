UpAssist:
  PrototypeGenerator:
    templates:
      properties:
        definition:
          default: |
            ${q(node).property('%s')}
          inlineEditable: |
            Neos.Neos:Editable {
              property = '%s'
            }
        rendering: |
          {props.%s}
